---
"@rapido-fab/baas-env": minor
---

Export `fixturePath` and `OPENCLAW_2026_6_11_PATH` from the package root, pin the fixture to OpenClaw 2026.6.11, and update the Claude CLI agent test defaults (v2 consolidation). Retroactive changeset for PR #5, which was merged without one and left the `main` CI red on the Require-changeset gate.
